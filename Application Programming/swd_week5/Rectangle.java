import java.util.Scanner;

public class Rectangle{

	public static void main(String[] args){

		static double Math.sqrt(double a );
		double value = Math.sqrt(9.0);

		public void area = double length * double width;
		result = length * width;
		System.out.println(length + " * " + width + " = " + result);

		public void perimeter = 2 * (double length * double width);
		result = 2 * length * width;
		System.out.println(2 * length + " * " + width + " = " + result);


		public void diagonal = Math.sqrt(double length * double length + double width * double width);
		result = length * length + width * width;
		System.out.println(length  * length + " + " + width * width + " = " + result);



	}
}